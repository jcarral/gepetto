const { QuestionConstants } = require('../constants/');
const { BOARD } = QuestionConstants;

const Main = [{
    name: BOARD.MAIN,
    type: 'list',
    message: 'Select one board',
    choices: [
    
    ]
  },];

  module.exports = {
      Main,
  }